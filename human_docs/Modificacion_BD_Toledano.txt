--2024-06-12 Monto mínimo por pedido
INSERT INTO P_MENUSISTEMA VALUES('1.4.9',1,'Monto mínimo pedidos','tsmiMontoMinimoPedidos',	2,'1.4')
INSERT INTO P_MENUROL VALUES(1, 3900, '1.4.9',1,1)
INSERT INTO P_MENUROL VALUES(13, 3903, '1.4.9',1,1)
INSERT INTO P_MENUROL VALUES(14, 3903, '1.4.9',1,1)
INSERT INTO P_MENUROL VALUES(41, 3905, '1.4.9',1,1)
INSERT INTO P_MENUROL VALUES(42, 3905, '1.4.9',1,1)
INSERT INTO P_MENUROL VALUES(27, 3904, '1.4.9',1,1)

--#CKFK20240606
ALTER TABLE D_PEDIDO ADD TIPO_PEDIDO nvarchar(10) null
GO

--#CKFK20240606
ALTER TABLE D_CLINUEVOT ADD RUC_VALIDADO BIT not null Default 0
GO

ALTER VIEW [dbo].[VW_CLIENTES_NUEVOS] AS
SELECT C.FECHA, C.RUTA, C.CODIGO,  C.NOMBRE, C.DIRECCION, 
C.NIT AS RUC, 
CASE WHEN C.IMAGEN IS NULL THEN 'NO' ELSE 'SI' END AS TIENE_IMAGEN,
S.EMPRESA, E.NOMBRE AS NOM_EMPRESA,
S.CODIGO AS CODIGO_SUCURSAL, S.NOMBRE AS NOM_SUCURSAL, C.CODIGO_ERP, C.STATCOM, 'D_CLINUEVO' AS TIPO,
'N/D' CANAL, 'N/D' SUBCANAL, 'ND' TIPOLOGIA, 0 RUC_VALIDADO
FROM D_CLINUEVO C INNER JOIN P_RUTA R ON C.RUTA = R.CODIGO
INNER JOIN P_SUCURSAL S ON R.SUCURSAL = S.CODIGO
INNER JOIN P_EMPRESA E ON S.EMPRESA = E.EMPRESA
UNION
SELECT C.FECHA, C.RUTA, C.CODIGO,  C.NOMBRE, C.DIRECCION, 
C.NIT AS RUC, 
CASE WHEN C.IMAGEN IS NULL THEN 'NO' ELSE 'SI' END AS TIENE_IMAGEN,
S.EMPRESA, E.NOMBRE AS NOM_EMPRESA,
S.CODIGO AS CODIGO_SUCURSAL, S.NOMBRE AS NOM_SUCURSAL, C.CODIGO_ERP, C.STATCOM, 'D_CLINUEVOT' AS TIPO,
ISNULL(N.CODIGO + ' ' + N.NOMBRE,'N/D') CANAL, 
ISNULL(B.CODIGO + ' ' + B.NOMBRE,'N/D') SUBCANAL,
ISNULL(T.CODIGO + ' ' + T.NOMBRE,'N/D') TIPOLOGIA,
C.RUC_VALIDADO
FROM D_CLINUEVOT C INNER JOIN P_RUTA R ON C.RUTA = R.CODIGO
INNER JOIN P_SUCURSAL S ON R.SUCURSAL = S.CODIGO
INNER JOIN P_EMPRESA E ON S.EMPRESA = E.EMPRESA
LEFT JOIN P_CANAL N ON N.CODIGO = C.CANAL
LEFT JOIN P_CANALSUB B ON B.CODIGO = C.SUBCANAL
LEFT JOIN P_TIPOLOGIA T ON T.CODIGO = C.TIPOLOGIA
GO

ALTER TABLE D_FACTURA_CONTROL_CONTINGENCIA
ADD [CORELATIVO] [nvarchar](50) NULL,
	[QRIMAGE] [image] NULL,
	[FECHA_AUTORIZACION] [datetime] NULL,
	[NUMERO_AUTORIZACION] [nvarchar](200) NULL ;  
GO
	
ALTER TABLE P_CODATEN
ADD ACTIVO BIT NOT NULL Default 0;
GO	

ALTER TABLE D_PEDIDO ADD TIPO_PEDIDO nvarchar(10) null
GO

ALTER TABLE D_PEDIDO
ADD TOTAL_MONTO_MINIMO  INT NOT NULL DEFAULT 0

http://movil.toledano.com/RDC7_SAP_QAS_ANDR/wsAndr.asmx

BA00020019165000158831 
